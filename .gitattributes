# Normalize to LF in the repo regardless of contributor OS. Without this,
# Windows checkouts commit CRLF and every file shows as fully rewritten.
* text=auto eol=lf

*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
